# Credentials, and the inventory itself — it holds the account's real phone
# numbers, so it must never be committed from a clone of this repo.
.env
twilio-inventory.json
twilio-inventory.csv
*-inventory.json
*-inventory.csv
node_modules/
